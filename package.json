{
  "name": "kanban",
  "version": "0.1.0",
  "description": "A personal Kanban board with the 8-Bit Atelier look: vanilla HTML/CSS/JS, no build step, IndexedDB persistence with a localStorage crash mirror, offline PWA.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/NXRJ/kanban.git"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "puppeteer": "^25.5.0"
  },
  "scripts": {
    "lint": "eslint js tests serve.js scripts sw.js",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:e2e": "node tests/kanban-smoke.js",
    "test": "npm run test:unit && npm run test:e2e",
    "serve": "node serve.js",
    "icons": "node scripts/gen-icons.js",
    "fonts": "node scripts/fetch-fonts.js"
  }
}
