{
  "name": "kanban",
  "version": "0.1.0",
  "description": "A personal Kanban board with seven switchable design worlds over one component layer: vanilla HTML/CSS/JS, no build step, IndexedDB persistence with a localStorage crash mirror, offline PWA.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/NXRJ/kanban.git"
  },
  "engines": {
    "node": ">=22.13.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "eslint": "^10.8.1",
    "puppeteer": "^25.5.0",
    "yjs": "^13.6.20"
  },
  "scripts": {
    "lint": "eslint js tests serve.js sync-relay.js scripts sw.js eslint.config.js",
    "test:unit": "node --test tests/unit/*.test.js",
    "test:e2e": "node tests/kanban-smoke.js",
    "test": "npm run test:unit && npm run test:e2e",
    "serve": "node serve.js",
    "serve:sync": "node serve.js --sync",
    "icons": "node scripts/gen-icons.js",
    "fonts": "node scripts/fetch-fonts.js",
    "yjs": "node scripts/fetch-yjs.js"
  }
}
